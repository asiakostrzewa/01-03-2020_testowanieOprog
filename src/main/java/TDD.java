public class TDD {

    public double powValue(double val, int pow) { //int bo potęga musi być typu całkowitego
        return Math.pow(val, pow);
    }

    public double divValue(double val1, double val2) {
            return val1 / val2;
    }

    public double sqrtValue(double val) {
        return Math.pow(val, 2);
    }
}
